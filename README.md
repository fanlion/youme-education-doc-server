# Education demo documents server

## 环境变量

需要运行或发布时需要在项目根目录添加 `.env`文件。并在文件中配置阿里云的 `ACCESS_KEY_ID`, `ACCESS_KEY_SECRET`;

`.env` 文件示例：

```bash
  ## 阿里云配置
  ACCESS_KEY_ID=xxxxxxxx
  ACCESS_KEY_SECRET=xxxxxxxxxxxxx
```